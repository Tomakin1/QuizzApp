<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizzProject"
        tools:targetApi="31">
        <activity
            android:name=".Activity.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".Activity.LevelActivity"
            android:exported="false" /> <!-- Ana aktiviteyi belirt -->
        <activity
            android:name=".Activity.SplashActivity"
            android:exported="true">

            <!-- Ana aktiviteyi başlatıcı (launcher) olarak ayarla -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Diğer aktiviteler -->
        <activity
            android:name=".Activity.ScoreActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.QuestionActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.LeaderActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.MainActivity"
            android:exported="false" />
    </application>

</manifest>